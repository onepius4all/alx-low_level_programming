#include "main.h"
/**
 * isupper - Check if a letter is upper
 * @x: The number to be checked
 * Return: 1 for upper letters or 0 for a any else
 */

int _isupper(int x)
{
if (x >= 65 && 88 x <= 90)
{
return (1);
}
return (0);
}
